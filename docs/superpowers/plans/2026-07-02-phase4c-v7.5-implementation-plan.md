# Phase 4C / V7.5 Implementation Plan

1. Add failing backend tests for scoped job visibility, safe retry, job
   lifecycle, model telemetry, token/error metrics, and document quality.
2. Add `IngestionJob`, `ModelInvocation`, and their migrations.
3. Centralize ingestion dispatch and update every producer.
4. Add scoped operations APIs and audit the retry action.
5. Add failing frontend API/dashboard tests, then expose queue, retry,
   model/token/error, and quality data.
6. Run Phase 4C focused tests and full backend/frontend gates.
7. Move Phase 4A/4B reports into `audit_reports/v7.5/`, create the Phase 4C
   V7.5 report, and replace V8.x release nodes in SPEC/progress/roadmap/plans.
8. Rename the local branch to `Version_7.5`, preserve user-owned worktree
   changes, and create a scoped local commit without pushing.
