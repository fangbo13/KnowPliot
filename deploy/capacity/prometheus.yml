global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: knowpilot-web
    metrics_path: /api/v1/internal/metrics/
    authorization:
      type: Bearer
      credentials: metrics-token-change-me
    static_configs:
      - targets: ["web:8000"]
